spring:
  application:
    name: product
  datasource:
   driver-class-name: com.mysql.jdbc.Driver
   username: root
   password: admin
   url: jdbc:mysql://127.0.0.1:3306/SpringCloud_sell?charcterEncoding=utf8&useSSL=false
  jpa:
   show-sql: true
eureka:
  client:
    service-url:
     defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${vcap.application.instance_id:${spring.application.instance_id:${random.value}}}:@project.version@